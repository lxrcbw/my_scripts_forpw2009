version: "3"
services:
  qinglong:
    image: lpssxs/ql295:latest
    container_name: ql
    restart: unless-stopped
    tty: true
    ports:
      - 9000:5700
      - 9101:5701
    environment:
      - ENABLE_HANGUP=true
      - ENABLE_WEB_PANEL=true
    volumes:
      - ./config:/ql/config
      - ./log:/ql/log
      - ./db:/ql/db
      - ./repo:/ql/repo
      - ./raw:/ql/raw
      - ./scripts:/ql/scripts
      - ./jbot:/ql/jbot
      - ./ninja:/ql/ninja